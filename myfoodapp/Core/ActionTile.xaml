<UserControl
    x:Class="myfoodapp.Core.ActionTile"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:myfoodapp.Core"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="myUserControl">

    <Grid Background="{Binding BackgroundColor,ElementName=myUserControl}">
        <Grid.RowDefinitions>
            <RowDefinition Height="2.5*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="1.2*"/>
        </Grid.ColumnDefinitions>
        <Image x:Name="image" Margin="5" Source="{Binding ImagePath,ElementName=myUserControl}"/>
        <TextBlock x:Name="textBlock" TextWrapping="Wrap" Text="{Binding ActionTitle,ElementName=myUserControl}" FontSize="8" FontWeight="Bold" Foreground="White" VerticalAlignment="Stretch" Margin="10,0,0,0" Grid.Row="1"/>
        <TextBlock x:Name="textBlock1" Grid.RowSpan="2" TextWrapping="Wrap" Text="{Binding ActionDescription, ElementName=myUserControl}" FontSize="10" Foreground="White" VerticalAlignment="Top" HorizontalAlignment="Stretch" Margin="2" FontFamily="Segoe UI Light" Grid.Column="1"/>
    </Grid>
</UserControl>
