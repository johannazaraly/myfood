<Page
    x:Class="myfoodapp.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:myfoodapp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{StaticResource AppBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Button Content="My Configuration" Style="{StaticResource OpenFormButton}" Click="{x:Bind ViewModel.OnMyConfigClicked}"/>
        <Button Grid.Column="1" Content="About" Style="{StaticResource OpenFormButton}" Click="{x:Bind ViewModel.OnAboutClicked}"/>
        <Button Grid.Row="1" Content="My Location" Style="{StaticResource OpenFormButton}" Click="{x:Bind ViewModel.OnMyLocationClicked}" IsEnabled="False"/>
        <Button Grid.Row="1" Grid.Column="1" Style="{StaticResource OpenFormButton}" Content="Register My Greenhouse" Click="{x:Bind ViewModel.OnRegisterClicked}" IsEnabled="False"/>
    </Grid>
</Page>
